/**
 * Editor state (Svelte 5 runes) and commands: selection, add/move/delete, rotate,
 * undo/redo, overlap detection, contextual assistant (click an open face -> compatible
 * pieces). Pure logic stays framework-free in `grid/`; this module only wires it to the UI.
 */
export {};
